import { Day } from "../Utils/CalendarUtils"; // Assuming the Day type is defined in CalendarUtils.ts

export const addEvent = (day: Day, title: string) => {
    // Implement the logic to add an event to the day
    console.log(`Adding event "${title}" to day ${day}`);
};